# App Data Directory

This directory stores application data including:

- `mission_cache.json` - Cached mission data for faster startup
- `solutions.db` - SQLite database for archived solutions (future)
